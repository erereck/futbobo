import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.futbobo.game",
  appName: "Futbobo",
  webDir: "out",
  backgroundColor: "#061710",
  android: {
    backgroundColor: "#061710",
  },
};

export default config;

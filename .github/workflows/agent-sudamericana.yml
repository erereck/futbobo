name: Temporary materializer (disabled)

# Temporary QA scaffold. Product changes are already committed.
# Remove this file before merging PR #4.
on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Remove before merge."
